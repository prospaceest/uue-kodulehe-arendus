
# AI-genereeritud blogimustandid (arendaja tööriist, mitte prod-sisu)
scripts/drafts/

# Regressioonikontrolli ajutised snapshotid — baasseis (.snapshots/ee-baas.json)
# on meelega repos, ülejäänud on ühekordsed võrdlusfailid.
.snapshots/*
!.snapshots/ee-baas.json

# Codemod'i väljavõte (tx() kaudu käivate eestikeelsete stringide loend).
# Genereeritakse skriptidest, tõlked ise elavad messages/{fi,sv}.json all.
.tx-extract.json
